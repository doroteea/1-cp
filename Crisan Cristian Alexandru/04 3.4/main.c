#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/*3.4. Write a program to generate
all the prime numbers less than or equal to a natural number, n
*/
void TestPrimeNumber(int x)
{
    for(int d=2;d<sqrt(x);)
}
int main()
{

    return 0;
}
