#include <stdio.h>
#include <stdlib.h>
#define SIZE 50
char substring()
{
    int i;
    char *originalString=(char*)malloc(sizeof(char*)*SIZE);
    char *subString=(char*)malloc(sizeof(char*)*SIZE);
    printf("\nPlease input a string of characters:");
    fgets(originalString,SIZE,stdin);

}




int main()
{
    printf("Hello world!\n");
    return 0;
}
