///neterminata ca e multa mate

#include <stdio.h>
#include <stdlib.h>

int main()
{
    typedef struct whatevs
    {
        int x;
        int y;
    }coordinates;
    int radius;

    coordinates center,a,b;
    printf("The coordinates of the center are: ");
    scanf("%d %d",&center.x,&center.y);
    printf("\nThe radius is: ");
    scanf("%d",&radius);
    printf("\nThe coordinates of A are: ");
    scanf("%d %d",&a.x,&a.y);
    printf("\nThe coordinates of B are: ");
    scanf("%d %d",&b.x,&b.y);


    return 0;
}
