#include <stdio.h>
int main()

{
    int number;
    scanf("%d",&number);
    printf("Decimal value=%d\n",number);
    printf("Octal value=%o\n",number);
    printf("Hexadecimal value=%X",number);
    return 0;
}
