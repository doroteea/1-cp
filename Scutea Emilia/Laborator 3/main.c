#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    int x,y,z=0;
    printf("Dati x:");
    scanf("%d",&x);
    printf("Dati y:");
    scanf("%d",&y);
    printf("%d",pow(x,y));
    return 0;
}
