#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("Size of char %d bytes",sizeof(char));
    printf("\nSize of short %d bytes",sizeof(short));
    printf("\nSize of long %d bytes",sizeof(long));
    printf("\nSize of int %d bytes",sizeof(int));
    printf("\nSize of longint %d bytes",sizeof(long int));
    return 0;
}
